# @design/assets Examples

- Run `pnpm sync:assets` to link/copy `packages/design-assets/assets` into each app's `public/assets`.
- Import assets by public path in your app, e.g. `/assets/images/logo.svg`.

